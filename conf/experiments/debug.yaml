# @package _global_
defaults:
  - /callbacks:
    - early_stopping
#     - model_checkpoint
#     - save_outputs

fit:
  min_steps: null
  validation_interval: 1

active_fit:
  max_rounds: 5
  validation_perc: null
  query_size: 25

active_data:
  budget: null
  validation_perc: 0.4

dataset_name: agnews_bert_tiny
experiment_group: dry_run
