# @package _global_
fit:
  min_steps: 100
  validation_interval: 5

active_fit:
  num_rounds: 50
  val_perc: 0.1
  query_size: 25

active_data:
  budget: 100
  val_perc: 0.5

dataset_name: imdb_bert_tiny
experiment_group: small_budget