# @package _global_
defaults:
  - /callbacks:
    - early_stopping
    - model_checkpoint
    - timer
    - grad_norm
    - save_outputs

callbacks:
  model_checkpoint:
    stage: train
    mode: max
    monitor: train/f1_class1
    save_last: false
  early_stopping:
    stage: train
    mode: max
    monitor: train/f1_class1

experiment_group: dry_run
