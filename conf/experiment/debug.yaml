# @package _global_
defaults:
  - /callbacks:
    - early_stopping
    - model_checkpoint
    - timer
    - grad_norm
    - save_outputs

fit:
  min_steps: 100
  validation_interval: 5

active_fit:
  max_budget: 5000
  validation_perc: 0.2
  query_size: 25

active_data:
  budget: 100
  validation_perc: 0.5

dataset_name: agnews_bert_tiny
experiment_group: debug
